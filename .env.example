# Safe V1 defaults: the tutor stays local and no conversation is sent externally.
EXPO_PUBLIC_TUTOR_API_ENABLED=false
TUTOR_API_ENABLED=false

# Server-only. Never expose this value through an EXPO_PUBLIC_ variable.
OPENAI_API_KEY=
