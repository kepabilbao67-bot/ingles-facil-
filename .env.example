# ---- Cliente (Vite) ----
# Si defines esto, el tutor y los pagos usan tu backend seguro en lugar de
# llamar directamente desde el navegador. Déjalo vacío para el modo demo.
VITE_API_BASE=

# ---- Servidor (funciones serverless en /api) ----
# Clave de Claude (NUNCA la pongas en el cliente)
ANTHROPIC_API_KEY=sk-ant-...

# Stripe (para cobrar de verdad)
STRIPE_SECRET_KEY=sk_live_o_test_...
STRIPE_PRICE_MONTHLY=price_xxx_mensual
STRIPE_PRICE_YEARLY=price_xxx_anual
